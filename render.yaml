services:
  - type: web
    name: sign-backend
    runtime: node
    buildCommand: npm install && pip install -r requirements.txt
    startCommand: node server.js
    envVars:
      - key: HOST
        value: "0.0.0.0"
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
